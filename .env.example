# Database - Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Backend Configuration (Server-side only)
# Service key for server-side operations (Settings > API > service_role key)
# ⚠️ NEVER expose this key in frontend code!
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Redis Configuration
# For session management and caching
# See REDIS_OPTIONS.md for detailed comparison of free vs paid options

# Option 1: Upstash Redis (⭐ RECOMMENDED for Production - FREE TIER)
# Sign up at https://upstash.com (10,000 commands/day free)
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token-here

# Option 2: Traditional Redis (Good for Development)
# Use Docker: docker run -d -p 6379:6379 redis:7-alpine
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Configuration
# Secret key for signing JWT tokens
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-change-in-production

# Cloudinary Configuration (Optional)
# Get these from your Cloudinary dashboard: https://cloudinary.com/console
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your-upload-preset-name
NEXT_PUBLIC_CLOUDINARY_FOLDER=toilet-inspections

# Midtrans Payment Gateway Configuration
# Get these from Midtrans Dashboard: https://dashboard.midtrans.com
# For testing: Use Sandbox keys (SB-Mid-...)
# For production: Use Production keys
MIDTRANS_IS_PRODUCTION=false
MIDTRANS_SERVER_KEY=SB-Mid-server-your-sandbox-server-key
MIDTRANS_CLIENT_KEY=SB-Mid-client-your-sandbox-client-key
NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=SB-Mid-client-your-sandbox-client-key

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Next.js Configuration
NODE_ENV=development
PORT=3000
